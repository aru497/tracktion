# OS
.DS_Store

# deps / logs
node_modules/
*.log

# generated by the crawler (refresh locally, don't commit)
data/parts.crawled.json

# local env / secrets — never commit
.env
.env.*

# vercel / supabase local
.vercel
supabase/.temp
supabase/.branches
